CREATE OBJECT secret_password (TYPE SECRET) WITH (value = "Qwerty12345!");

CREATE EXTERNAL DATA SOURCE Redis WITH (
    SOURCE_TYPE="Redis",
    LOCATION="localhost:1433",
    DATABASE_NAME="test",
    AUTH_METHOD="BASIC",
    LOGIN="SA",
    PASSWORD_SECRET_NAME="secret_password",
    PROTOCOL="NATIVE",
    USE_TLS="FALSE"
);